version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - cd server && npm install
    build:
      - cd server && npx prisma generate
run:
  command: cd server && npm start
  network:
    port: 5000
    env: PORT
  env:
    - name: NODE_ENV
      value: "production"
